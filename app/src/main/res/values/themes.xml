<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Deliberately parented on the platform dark theme rather than AppCompat: the whole
      UI is Compose, and dragging in the Material XML theme stack would only add weight.
    -->
    <style name="Theme.CoverDeck" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/deck_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <!--
      See-through window for cover recents: the app underneath stays visible (blurred and
      dimmed by the activity itself), and it fades rather than sliding in.
    -->
    <style name="Theme.CoverDeck.Recents" parent="Theme.CoverDeck">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@style/Animation.CoverDeck.Recents</item>
    </style>

    <style name="Animation.CoverDeck.Recents" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@android:anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@android:anim/fade_out</item>
    </style>
</resources>
